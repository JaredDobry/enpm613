export type TicketType = {
  id: string;
  title: string;
  assignee: string;
  description: string;
};

export type TicketColumnType = {
  id: string;
  title: string;
};

export type BoardType = {
  id: number;
  title: string;
};
