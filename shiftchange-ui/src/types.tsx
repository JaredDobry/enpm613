export type AssignmentType = {
  id: string;
  title: string;
  description: string;
};

export type ColumnType = {
  id: string;
  title: string;
};

export type BoardType = {
  id: number;
  title: string;
};
